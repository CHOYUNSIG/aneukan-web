export class Me {
  id: number;
  userId: string;
  name: string;
  serialNumber: string;

  constructor(id: number, userId: string, name: string, serialNumber: string) {
    this.id = id;
    this.userId = userId;
    this.name = name;
    this.serialNumber = serialNumber;
  }
}
