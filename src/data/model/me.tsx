export class Me {
  key: number;
  id: string;
  name: string;
  serialNumber: string;

  constructor(key: number, id: string, name: string, serialNumber: string) {
    this.key = key;
    this.id = id;
    this.name = name;
    this.serialNumber = serialNumber;
  }
}
